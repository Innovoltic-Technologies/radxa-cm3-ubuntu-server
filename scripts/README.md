# Scripts

The scripts in this directory provide lightweight repository and release validation.

## verify-image.sh

Verifies the SHA256 checksum of the v1.0.0 release image.

## validate-tree.sh

Checks that required repository files are present.

## inspect-image.sh

Displays basic information about the release disk image and its partition table.

The scripts do not modify the release image.
